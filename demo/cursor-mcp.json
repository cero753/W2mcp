{
  "//": "w2mcp MCP configs — proves a REAL agent (Cursor, not Claude Code) uses your servers with zero custom code.",
  "//how": "Copy into  %USERPROFILE%\\.cursor\\mcp.json  (global) or  <project>\\.cursor\\mcp.json, then Cursor > Settings > MCP and toggle a server on.",
  "//the-hero": "'w2mcp-hub' = ONE connection that exposes the tools of ALL registered APIs (coingecko/frankfurter/httpbin/chuck), namespaced. Turn it on and ask across APIs in one breath.",
  "mcpServers": {
    "w2mcp-hub": {
      "command": "node",
      "args": ["--import", "tsx", "C:\\Users\\karti\\anymcp\\src\\hub.ts"]
    },
    "coingecko-hosted": {
      "url": "http://localhost:8080/mcp/coingecko",
      "headers": { "Authorization": "Bearer ak_demo_w2mcp_2026" }
    },
    "chuck-local": {
      "command": "node",
      "args": ["--import", "tsx", "C:\\Users\\karti\\anymcp\\out\\chuck-demo\\server.ts"]
    }
  }
}
